# 기여 안내

이 저장소는 cycle 체계로 굴러간다. 백로그는 GitHub Issues, 작업 단위는 사이클(마일스톤 = 버전), 문서는 `docs/cycles/`에 남는다.

## 이슈

- 하나의 이슈는 하나의 일. 제목은 결과가 보이게("~가 되게 한다"), 본문은 이슈 템플릿대로.
- 우선순위는 라벨 `p1`(지금) `p2`(다음) `p3`(언젠가). 착수 조건이 있으면 `hold`와 함께 조건을 코멘트로.
- 본문은 고치지 않고 코멘트를 단다. 이력이 남아야 한다.
- 닫을 때 `completed`는 끝난 것, `not planned`는 하지 않기로 한 것. 후자는 사람이 정한다.

## 브랜치

- `main` 릴리즈. 태그로만 전진한다.
- `develop` 통합. 작은 수정은 여기 바로 커밋한다. 이슈에서 나온 것이면 커밋 메시지에 `closes #n`.
- `{버전}-{사이클명}` 사이클 작업. 닫힐 때 `develop`에 머지되고 머지 커밋에 태그가 붙는다.

## 사이클

`/cycle:propose → /cycle:plan → /cycle:do → /cycle:close`. 각 단계의 절차는 스킬이 정본이다. 이 프로젝트의 검증 명령 · 브랜치 · 훅은 `CLAUDE.md`의 `cycle` 절에 있다.

## 커밋 메시지

`{단계}: {한 줄}`. 단계는 `scope` `plan` `do` `close` 또는 일반 접두(`fix` `docs` `chore`).

## 캐릭터 이미지

1. `character-spec.md`의 공통 프롬프트에 해당 상태의 표정 문장을 붙여 이미지 생성기에 넣는다.
2. 결과물을 512px 투명 PNG로 맞춰 `img/`에 상태 이름으로 넣는다. 생성기가 격자 배경을 그려 주면 지운다.
3. 없는 상태의 이미지는 `idle.png`로 대체되므로, 한 상태만 바꿔도 나머지는 그대로 돈다.

## 테스트 픽스처

`~/.claude/projects/`의 원본 트랜스크립트를 그대로 커밋하지 않는다. 경로 · 프롬프트 본문 · 토큰을 지우고 집계에 필요한 수치 필드만 남긴 합성 `.jsonl`을 `test/fixtures/`에 넣는다. 픽스처는 `test/helper.js`의 `runFixture(name)`으로 읽는다.
